"""pixelforge -- a text-first pixel art pipeline.

Sprites live as .pxa text (palette + ASCII grid) so they can be authored and
edited like source, then rasterized to PNG/GIF with no third-party deps.
"""

__version__ = "1.0.0"
