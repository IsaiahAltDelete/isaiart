# Claude Code / agent + preview artifacts (must never be deployed to Pages)
.claude/
**/.claude/
**/worktrees/
*.log
update_repos.log
.DS_Store

# source zip for /dndgame — unpacked, no need to ship it
DND GAME.zip

# python bytecode
__pycache__/
*.pyc
